<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JSON Parser</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            margin: 20px;
        }

        .container {
            max-width: 800px;
        }

        .result-box {
            background-color: #f8f9fa;
            border: 1px solid #ddd;
            padding: 15px;
            border-radius: 5px;
            white-space: pre-wrap;
            margin-top: 20px;
            font-family: 'Courier New', Courier, monospace;
        }

        .error-box {
            color: red;
            margin-top: 20px;
        }

        .file-upload {
            margin-top: 20px;
        }

        .back-btn {
            margin-top: 20px;
        }

        footer {
            margin-top: 40px;
            text-align: center;
            font-size: 14px;
        }
    </style>
</head>
<body>
<div class="container">
    <h1 class="my-4 text-center">JSON Parser</h1>

    <% if (!parsedData && !error) { %>
        <div class="file-upload">
            <form action="/upload" method="POST" enctype="multipart/form-data" class="mb-4">
                <div class="mb-3">
                    <label for="formFile" class="form-label">Upload a JSON Text File</label>
                    <input class="form-control" type="file" name="file" accept=".txt" required>
                </div>
                <button type="submit" class="btn btn-primary w-100">Upload and Parse</button>
            </form>
        </div>
    <% } %>

    <% if (error) { %>
        <div class="alert alert-danger error-box">
            <h4>Error</h4>
            <p><%= error %></p>
        </div>
    <% } %>

    <% if (parsedData) { %>
        <div class="result-box">
            <h4>Parsed Data</h4>
            <div><%= parsedData %></div>
        </div>
    <% } %>

    <% if (parsedData || error) { %>
        <div class="text-center back-btn">
            <a href="/" class="btn btn-secondary">Back</a>
        </div>
    <% } %>
</div>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
